import { headers } from '../styles';

const Users = () => {
  return (
    <div className="w-full h-full p-5 overflow-auto">
      <h1 className={headers.h1}>Benutzer</h1>
    </div>
  );
};

export default Users;
